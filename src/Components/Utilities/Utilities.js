const add = (first,second) =>{
    return first + second;
}

const sub = (first,second) =>{
    return first - second;
}

const multi = (first,second)=>{
    return first * second;
}
export { add, sub, multi };

